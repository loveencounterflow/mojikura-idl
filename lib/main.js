// Generated by CoffeeScript 1.10.0
(function() {
  var CND, MKNCR, alert, badge, color, debug, echo, glyph, help, i, info, is_cjk, is_ideograph, is_idl, j, len, len1, log, nfo, ref, ref1, rpr, tags, tags_txt, text, texts, urge, warn, whisper,
    indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'MOJIKURA/IDL';

  log = CND.get_logger('plain', badge);

  info = CND.get_logger('info', badge);

  whisper = CND.get_logger('whisper', badge);

  alert = CND.get_logger('alert', badge);

  debug = CND.get_logger('debug', badge);

  warn = CND.get_logger('warn', badge);

  help = CND.get_logger('help', badge);

  urge = CND.get_logger('urge', badge);

  echo = CND.echo.bind(CND);

  MKNCR = require('mingkwai-ncr');

  if (module.parent == null) {
    texts = ['⿺辶言', '⿺廴聿123'];
    for (i = 0, len = texts.length; i < len; i++) {
      text = texts[i];
      ref = MKNCR.chrs_from_text(text);
      for (j = 0, len1 = ref.length; j < len1; j++) {
        glyph = ref[j];
        nfo = MKNCR.describe(glyph);
        tags = (ref1 = nfo.tag) != null ? ref1 : [];
        tags_txt = tags.join(' ');
        is_cjk = indexOf.call(tags, 'cjk') >= 0;
        is_ideograph = indexOf.call(tags, 'ideograph') >= 0;
        is_idl = indexOf.call(tags, 'idl') >= 0;
        color = CND.grey;
        if (is_cjk) {
          color = CND.gold;
        }
        if (is_ideograph) {
          color = CND.lime;
        }
        if (is_idl) {
          color = CND.pink;
        }
        help(color(glyph));
      }
    }
  }

}).call(this);

//# sourceMappingURL=main.js.map
